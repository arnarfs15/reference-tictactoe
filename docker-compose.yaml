version: '2'
services:
  server:
    environment:
      - 'PGHOST=NODE_ENV'
      - 'PGPORT=5432'
      - 'PORT=80'
    image: 'arnarfs15/reference-tictactoe'
    ports:
      - '8080:8080'
      - '80:80'
    depends_on:
      - 'postgres'
  postgres:
    image: 'postgres'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=mysecretpassword
    ports:
      - '5432:5432'
