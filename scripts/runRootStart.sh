#!/bin/bash

npm run startdockerdb       #Starts the database if needed, if there's time will have an if statement to check if the connection doesn't exist
npm run start-dev           #Runs start for a development environment
